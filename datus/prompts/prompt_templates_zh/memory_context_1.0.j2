{# 启用记忆时注入系统提示词的记忆上下文 #}
{% if has_memory %}
{% if read_only %}
## 记忆（来自 {{ originating_agent }} 的只读继承）

{{ originating_agent }} 的持久记忆完整内联在下方，作为父代理收集的关于用户偏好、项目决策和外部引用的只读背景信息。

**只读**：你没有记忆写入工具。不要尝试修改 `.datus/memory/` 下的任何路径——该子树对文件系统工具是隐藏的。如果你发现用户可能希望被记住的内容，在回复中提及，以便父代理持久化它。

### 当前记忆

<memory>
{% if memory_content %}
{{ memory_content }}
{% else %}
（空）
{% endif %}
</memory>

**使用**：当这些事实与当前任务相关时应用它们。在行动前对照当前代码核实文件 / 函数 / 标志的说法——相信你现在观察到的东西。如果用户要求忽略记忆，将其视为空。
{% else %}
## 记忆

持久记忆是下面展示的单个文件。随时间构建它，让未来的会话了解用户的偏好、项目上下文和外部引用。

**保存**（不明显、可复用的事实）：用户角色 / 目标 / 风格；纠正（"停止 X"）和默许确认（"正是如此"）——用 **规则 → 原因：→ 如何应用：** 包含*为什么*；不在代码 / git 中的项目决策 / 截止日期（将相对日期转换为绝对日期）；外部系统（追踪器、仪表板、渠道）。

**不要保存**：代码模式、文件路径、git 历史、调试修复、CLAUDE.md 或 `data_context/` 中的任何内容、临时任务状态。如果被要求保存 PR 列表，改为询问有什么*令人意外*的发现。

**方式**：记忆是一个硬性 **2000 字节**上限的单个扁平文件——没有主题文件，没有索引。只使用专用工具：`add_memory(content)` 追加一条简洁的事实，`edit_memory(old_string, new_string)` 更新或删除（传空的 `new_string` 表示删除）。不要在 `.datus/memory/` 上使用 `write_file`/`edit_file`/shell——该子树对文件系统工具是隐藏的。写得简洁，每次调用一条事实，写入前先阅读下方当前记忆——绝不重复。当 `add_memory` 报告文件已满时，先用 `edit_memory` 删除一条过时条目释放空间，然后重试。

### 当前记忆

<memory>
{% if memory_content %}
{{ memory_content }}
{% else %}
（空）
{% endif %}
</memory>

**使用**：当记忆看起来相关或用户提到过去的工作时；当用户要求回忆时必须访问。在行动前对照当前代码核实文件 / 函数 / 标志的说法——相信你现在观察到的东西，并用 `edit_memory` 更新过时条目。对于当前仓库状态，优先使用 `git log`/代码。如果用户说忽略记忆，将其视为空。

使用**计划**进行实现对齐，使用**任务**跟踪当前工作——而不是记忆。
{% endif %}
{% endif %}
