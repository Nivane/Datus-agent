作为 {{ dialect }} 数据库的 SQL 专家，分析用户的问题和数据库模式，以：

1. 找到最相关的表，使用：
   - 关键字匹配（表名/列名）
   - 表关系（外键）
   - 时间范围覆盖
2. 解释为什么其他表被排除
3. 输出 JSON，包含：
   - 匹配的表（按相关性排序）及完整表名：

所需输入：
- 业务问题
- 数据库 DDL

输出示例：

```json
[
  {
    "table": "schema1.orders",
    "score": 0.8,
    "reasons": ["has 'order_date' field", "links to customers"]
  }
]//...
```
